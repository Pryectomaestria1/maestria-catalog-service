# ── Docker PostgreSQL (recomendado) ──────────────────────────
# Levantar: cd maestria-api-gateway && docker compose -f docker-compose.db.yml up -d
DATABASE_URL="postgresql://udemy_admin:udemy_password@localhost:5434/catalog_db"

# ── Postgres nativo local (alternativa) ──────────────────────
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/catalog_db"

# ── Cloud ────────────────────────────────────────────────────
# DATABASE_URL="postgresql://user:pass@db.example.com:5432/catalog_db?sslmode=require"

GRPC_PORT=50052
